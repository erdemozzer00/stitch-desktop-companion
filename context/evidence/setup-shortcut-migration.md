# Recipient setup shortcut migration — 2026-09-24

The recipient screenshot shows Kur.ps1 line 10 rejecting an existing current-user Startup/Stitch.lnk because its target differs from the new `Desktop/stitch/Hediye/StitchPet.exe`. The old guard incorrectly conflated a previous installation path with an unrelated application. The earlier advice to keep the old folder as backup and run the new Kur.cmd did not account for this guard.

Updated the self-contained setup script to validate both destinations before writing either link. A differing target is recognized as a prior installer-owned link only if its filename is StitchPet.exe, Description is Stitch, arguments are empty and working directory matches the target parent. This supports an absent/moved old folder without executing or deleting its contents. Existing matching links and clean installations still work; unrelated links still stop the operation before shortcut writes. Retargeted .lnk files are copied under the new package's `.shortcut-backups/<unique id>/` before replacement.

`scripts/check_package_shortcuts.ps1` loads the unchanged production function through its parsed AST and executes it against isolated, real WScript.Shell .lnk files under `.local/phase-09/`. Windows PowerShell checks passed: fresh install, idempotent reinstall, migration from a removed old directory, byte-identical shortcut backups, and an unrelated second link leaving both links unchanged. The developer's real Desktop/Startup folders were not modified by tests. Recipient Windows 11 retry remains pending.

Installed byte-identical setup script as `C:/Users/Erdem/Desktop/Hediye/Kur.ps1`; updated the usage note. Prior Kur.ps1 and usage note are preserved in `.local/phase-09/setup-fix-backup-20260923-213516`. StitchPet.exe remains unchanged. Prepared `C:/Users/Erdem/Desktop/Stitch-Kurulum-Duzeltmesi.zip` containing only Kur.ps1; no model or app binary transfer is needed. The recipient should close the old Stitch, extract the ZIP, replace Kur.ps1 inside the new gift folder and run its existing Kur.cmd. Running the patch script from outside the gift folder is not the installation procedure.
